# FieldMesh app — server configuration (public values only; never secrets).
# With no .env at all the app uses the AWS Lightsail server built into src/lib/config.ts.
# These values are baked into the bundle at build time (EXPO_PUBLIC_* only).
EXPO_PUBLIC_API_URL=http://98.80.162.7:3000
EXPO_PUBLIC_WS_URL=ws://98.80.162.7:1234
# Local development against `pnpm dev:server` on this machine (Expo dev-server host, :3000/:1234):
#EXPO_PUBLIC_API_URL=dev
