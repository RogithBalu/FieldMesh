# Baked into the app bundle at build time (EXPO_PUBLIC_* only; never secrets).
# Points at the AWS Lightsail server. Change and rebuild, or override at runtime from the app's Site Session screen.
EXPO_PUBLIC_API_URL=http://98.80.162.7:3000
EXPO_PUBLIC_WS_URL=ws://98.80.162.7:1234
